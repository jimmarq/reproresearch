# This file runs all of the files separately.
# This should not be referenced in the Makefile because it will force all
# files to be re-run, even if they have not changed.

source("load.R")
source("clean.R")
source("functions.R")
source("tree_analysis.R")
